#include <unordered_map>
#include <string>

namespace target
{
    using json_t = std::unordered_map<std::string,

class Target
{
public:
    explicit Target() = default;
    virtual ~Target() = default;
    virtual void add_line(
};




}
